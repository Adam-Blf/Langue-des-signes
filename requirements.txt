# ===================================================================
# Langue des Signes - Dépendances Python
# AI-Powered Sign Language Detection Platform
# ===================================================================

# Core - Computer Vision & ML Base
mediapipe>=0.10.0          # Hand detection with 21 3D landmarks
opencv-python>=4.8.0       # Video capture and image processing
numpy>=1.26.0              # Numerical computing
pandas>=2.2.0              # Data manipulation
scikit-learn>=1.4.0        # RandomForest model (CPU baseline)
joblib>=1.3.0              # Model serialization and caching
Pillow>=10.0.0             # Image processing utilities

# New Features - TTS Voice Feedback
pyttsx3>=2.90              # Text-to-speech synthesis (offline)
pywin32>=305; sys_platform == 'win32'  # Windows TTS backend

# New Features - GPU Acceleration (Optional)
# Uncomment based on your hardware:

# NVIDIA GPU (CUDA)
# torch>=2.0.0
# torchvision>=0.15.0
# onnxruntime-gpu>=1.16.0

# Apple Silicon (Metal)
# torch>=2.0.0
# torchvision>=0.15.0

# CPU-only (fallback)
# onnxruntime>=1.16.0

# Model Conversion Tools
skl2onnx>=1.15.0           # Convert scikit-learn to ONNX

# Development & Testing
pytest>=8.0.0              # Unit testing framework
pytest-cov>=4.1.0          # Code coverage reports
black>=23.0.0              # Code formatter
flake8>=6.0.0              # Linting
mypy>=1.7.0                # Type checking

# ===================================================================
# Installation Instructions:
# 
# Base installation (CPU):
#   pip install -r requirements.txt
#
# GPU - NVIDIA CUDA:
#   pip install -r requirements.txt
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   pip install onnxruntime-gpu
#
# GPU - Apple Silicon:
#   pip install -r requirements.txt
#   pip install torch torchvision
#
# Development:
#   pip install -r requirements.txt
#   pip install pytest-cov black flake8 mypy
# ===================================================================

